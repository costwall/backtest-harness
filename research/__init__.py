"""
research/ — переиспользуемый харнесс проверки сигналов против cost-wall.
================================================================================
Цель (D 2026-06-15): прекратить писать одноразовые scratch-скрипты под каждую
гипотезу. Новый кандидат = чистая функция df -> Series в signals/, прогон через
costwall.evaluate, строка вердикта в RESULTS.md. См. память
feedback-reusable-research-base.

Слои:
  data.py      — каноничные загрузчики (liquidity_bot deriv + наши коллекторы + forex OHLC)
  costwall.py  — ЕДИНСТВЕННЫЙ оценщик: AUC + размер движения vs costs + net, causal split
  signals/     — реестр сигналов-кандидатов (pure df -> Series)
  run.py       — CLI: символ × сигнал × горизонт → RESULTS.md

Инвариант: НИКАКОГО look-ahead. fwd_return смотрит в будущее ТОЛЬКО как метка;
сигнал считается строго на прошлом; знак ориентируется на train, метрики — на test.
"""
